<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MotoAPP.Views.MinhasComprasView"
             Title="Meus Consórcios">

    <StackLayout Padding="20">
        <Label Text="Meus Consórcios Ativos" FontSize="24" FontAttributes="Bold"/>

        <ListView ItemsSource="{Binding MinhasCompras}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame Padding="10" Margin="0,5" CornerRadius="5">
                            <StackLayout>
                                <Label FontAttributes="Bold" FontSize="18">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Moto: "/>
                                            <Span Text="{Binding Moto.Descricao}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label>
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Valor: R$ "/>
                                            <Span Text="{Binding ValorCompra, StringFormat='{0:N2}'}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </StackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Text="Voltar" Command="{Binding CommandVoltar}" Margin="0,20,0,0"/>
    </StackLayout>

</ContentPage>